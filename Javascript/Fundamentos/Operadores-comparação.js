// Variáveis de exemplo

let maior = 50;
let menor = 20;
let valor_x = 20;

// '>' verifica se o número da esquerda é maior que o da direita.

let verdade = maior > menor;
console.log("É maior?", verdade);

// '<' verifica se o número da esquerda é menor que o da direita.

let inverdade = maior < menor;
console.log("É menor?", inverdade);

// '>=' verifica se o número da esquerda é maior ou igual ao da direita.

let igual = menor >= valor_x;
console.log("É maior ou igual?", igual);

// '<=' verifica se o número da esquerda é menor ou igual ao da direita.

let igual_menor = menor <= valor_x;
console.log("É menor ou igual?", igual);

// "===" verifica se os valores são iguais

let igualdade = menor === valor_x;
console.log("Tem o mesmo valor?", igualdade);

//  '!==' verifica se os valores são diferentes

let diferente = menor !== maior;
console.log("São diferentes?", diferente);

